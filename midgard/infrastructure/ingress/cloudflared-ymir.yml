# Gjallarhorn — the `ymir` tunnel. Hosts the Hlidskjalf control plane.
#
#   ymirdell.zerwiz.org  ->  http://127.0.0.1:3888  (Hlidskjalf SPA; it proxies /api)
#
# Created with:  cloudflared tunnel create ymir
#                cloudflared tunnel route dns --overwrite-dns <id> ymirdell.zerwiz.org
#
# SECURITY: Hlidskjalf's auth is a mock until Heimdall (oauth2-proxy) lands.
# Put an authenticating origin (Bifrost → Heimdall) in front before broad
# public exposure; until then this hostname is open.
tunnel: ymir
credentials-file: /home/zerwiz/.cloudflared/c9a65c62-7f1b-4fe6-bcdd-9558570eefd9.json

ingress:
  - hostname: ymirdell.zerwiz.org
    service: http://127.0.0.1:3889
    originRequest:
      noTLSVerify: true
  - service: http_status:404
