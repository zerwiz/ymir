# `bin/` — what every file does

Auto-generated from each script's header. One line per tool. `fm-*` files are
ported from firstmate (the agent control plane); the rest are Ymir's own.

bin[101]{file,does}:
  "a2a-mcp.sh","a2a-mcp.sh — install/verify the two A2A MCP servers into the harnesses so"
  "a2a-serve.py","a2a-serve.py — a real A2A 1.0 server (a2a-sdk) whose agent is a seated Eindri:"
  "a2a-serve.sh","a2a-serve.sh — run the a2a-sdk A2A 1.0 server for a seated Eindri."
  "a2a-talk.sh","a2a-talk.sh — list A2A agents and talk to one (bidirectional reply)."
  "agent-run.sh","agent-run.sh — run one agent's errand through the harness the Allfather chose."
  "agents-config.sh","agents-config.sh — one YAML for the Allfather's agent/harness/model combination."
  "bifrost-bridge.sh","bifrost-bridge.sh — raise/lower the model bridge (Bifrost: the bridge between"
  "bifrost-ingress.sh","bifrost-ingress.sh — raise/lower the ingress stack (Bifrost · Heimdall · Gjallarhorn)."
  "brokk","brokk — the primary agent's CLI (Brokk, the bellows-smith)."
  "brokk-classify-lib.sh","tests, declared-external-wait vocabulary, and the working/paused absorb"
  "brokk-lease-lib.sh","brokk-lease-lib.sh - the per-task supervision lease contract (one owner)."
  "brokk-lease.sh","brokk-lease.sh - claim, release, inspect, and sweep per-task supervision leases."
  "brokk-lint.sh","brokk-lint.sh — the Brokk lint gate. Single owner of lint for CI and"
  "brokk-send.sh","brokk-send.sh — send a one-line steer to a running Brokk/Eindri-home."
  "brokk-update.sh","brokk-update.sh — update Brokk and every registered Eindri-home to the latest."
  "brokk-wake-grant.sh","shellcheck source=bin/brokk-wake-lib.sh"
  "brokk-wake-lib.sh","Shared durable wake queue and portable lock helpers."
  "crash-sense.sh","A crash nobody sees is a crash nobody fixes. This watches systemd-coredump for"
  "desktop-place.sh","desktop-place.sh — put the Ymir apps on their own Hyprland desktops."
  "docs-guard.sh","docs-guard.sh — docs/ is the PUBLIC, user-facing tree. Block operator-private"
  "editor-place.sh","editor-place.sh — the editor opens on its OWN desktop, never on yours."
  "eindri-control.sh","eindri-control.sh — the CONTROL plane: allowlisted lifecycle verbs for a"
  "eindri-role.sh","eindri-role.sh — the right smith for the right metal."
  "eindri-send.sh","eindri-send.sh — the DATA plane: send conversational text to a seated Eindri."
  "eindri-start.sh","eindri-start.sh — start an Eindri as the Allfather asks. One command."
  "einherjar-spawn.sh","einherjar-spawn.sh - gather an Eindri worker (Einherjar = the chosen who are"
  "erindi-brief.sh","erindi-brief.sh - scaffold an Eindri worker brief at data/<task-id>/brief.md."
  "fleet-apply.sh","fleet-apply.sh — apply fleet-wide preferences to every registered Eindri-home."
  "github-deploy.sh","github-deploy.sh — zero-trust deploys (W0035). Sync secrets from the local env"
  "gjallarhorn-notify.sh","gjallarhorn-notify.sh — the horn (W0036). Unified push: Telegram + a web/WS"
  "gjallarhorn-purge.sh","gjallarhorn-purge.sh — purge the Cloudflare edge cache for a hostname."
  "gjallarhorn-tunnel.sh","gjallarhorn-tunnel.sh — raise/lower the `ymir` Cloudflare tunnel (Gjallarhorn)."
  "gleipnir-lock-lib.sh","gleipnir-lock-lib.sh - per-home session lock for the Brokk runtime."
  "hamr-harness.sh","hamr-harness.sh - detect the agent harness (Hamr) this process tree runs on."
  "heimdall-ssh-keys.sh","heimdall-ssh-keys.sh — Heimdall's SSH-keys surface."
  "herdr-agents.py","(no header description)"
  "herdr-ensure.sh","herdr-ensure.sh — Þjazi: guarantee the terminal backend Ymir needs."
  "herdr-run.sh","herdr-run.sh — raise an Eindri in herdr, by the grain that fits the errand."
  "hermes-ensure.sh","hermes-ensure.sh — ensure the Hermes agent runtime is present."
  "hodd.sh","hodd.sh — the Allfather's private hoard: path, init, ls, load, tenant."
  "local-model-lock.sh","local-model-lock.sh — serialize LOCAL model inference on one machine."
  "mimir-bridge.py","(no header description)"
  "mimir-bridge.sh","mimir-bridge.sh — raise/lower the well bridge (Mimirsbrunn on :4602)."
  "mimir-ingest.sh","mimir-ingest.sh — ingest the repo's data material into the well (Mimirsbrunn)."
  "mimir.sh","mimir.sh — the well (Mimirsbrunn). Drink before you act, water it after."
  "mjollnir-webhook.sh","mjollnir-webhook.sh — HMAC-verified GitHub webhook → Mjollnir (W0018)."
  "mjollnir.sh","mjollnir.sh — the hammer (W0018). An issue becomes a PR: read the issue, cut a"
  "model-resolve.sh","model-resolve.sh — resolve a human model request to a concrete harness+model."
  "nornir-cron-start.sh","nornir-cron-start.sh - ensure Brokk's scheduled jobs are running (idempotent)."
  "nornir-job-daily-briefing.sh","nornir-job-daily-briefing.sh - the 07:00 Sága daily seeing."
  "nornir-job-git-sync.sh","nornir-job-git-sync.sh - Yggdrasil, the world-tree kept in order."
  "nornir-job-memory-housekeeping.sh","nornir-job-memory-housekeeping.sh - Muninn, the raven of memory."
  "nornir-job-observer.sh","nornir-job-observer.sh - Huginn, the raven of observation."
  "omarchy-hook-install.sh","omarchy-hook-install.sh — let Ymir notice when Omarchy updates."
  "omarchy-install.sh","omarchy-install.sh — the Omarchy installation layer (Rule 05)."
  "omarchy-plugins.sh","omarchy-plugins.sh — the Omarchy plugins Ymir suggests, and installs on consent."
  "omarchy-sense.sh","omarchy-sense.sh — learn the Allfather's Omarchy machine."
  "perm-guard.sh","perm-guard.sh — flag agent profiles whose permissions are self-defeating."
  "pi-agent.sh","pi-agent.sh — run a Ymir agent on a LOCAL model with ITS OWN persona/mission"
  "pi-local.sh","pi-local.sh — run a Pi agent on a LOCAL model, reusable."
  "pi-seat.sh","pi-seat.sh — seat a Pi agent in a VISIBLE herdr pane, optionally on a local"
  "prereq-ensure.sh","prereq-ensure.sh — self-healing prerequisite engine for Ymir."
  "private-guard.sh","private-guard.sh — enforce the Hoard boundary (Rule 04). Nothing under a"
  "project-git.sh","project-git.sh — resolve a project's GitHub block from the master registry"
  "public-guard.sh","public-guard.sh — every PUBLIC, user-facing file must be free of operator"
  "ratatoskr.sh","ratatoskr.sh — Ymir's front door to the A2A engine (`a2abridge`)."
  "repo-scrub.sh","repo-scrub.sh — purge private paths from ALL git history before going public."
  "rodd-operational-input.sh","rodd-operational-input.sh - canonical Rödd operational-input protocol."
  "runes-append.sh","runes-append.sh - append-only Runes audit ledger."
  "saga-session-start.sh","saga-session-start.sh - the one-command Brokk session start."
  "saga-sessionstart-run.sh","saga-sessionstart-run.sh - session-open entry point for run-tier harnesses."
  "saga-wake-drain.sh","saga-wake-drain.sh - present durable Brokk wakes."
  "secret-guard.sh","secret-guard.sh — refuse to commit obvious secrets or private env files."
  "skuld-branch-outcome.sh","skuld-branch-outcome.sh - the durable outcome store for the Pi supervision"
  "skuld-branch-prompt.sh","skuld-branch-prompt.sh - emit the supervision branch's system prompt"
  "smidja-bootstrap.sh","smidja-bootstrap.sh — make the Smíðja visualizer ready on a fresh install."
  "smidja-observe.sh","smidja-observe.sh — the smithy's trace, read-only (W0075). Reads the REPO's own"
  "syn-arm-pretool-check.sh","syn-arm-pretool-check.sh - PreToolUse seatbelt for watcher-arm bash commands."
  "syn-asset-pretool-check.sh","syn-asset-pretool-check.sh — PreToolUse seatbelt for governed paths."
  "syn-cd-pretool-check.sh","syn-cd-pretool-check.sh - PreToolUse seatbelt for directory-changing commands."
  "syn-guard-pretool-check.sh","syn-guard-pretool-check.sh — PreToolUse seatbelt for the runtime's load-bearing invariants."
  "syn-turnend-guard.sh","syn-turnend-guard.sh - refuse to let a Brokk turn end blind."
  "syn-watch-arm.sh","syn-watch-arm.sh - arm one Brokk supervision watcher cycle."
  "tailscale-sync.sh","tailscale-sync.sh — sync the Allfather's pi data across his OWN machines over"
  "telegram-bot.sh","telegram-bot.sh — the horn answers (W0036). Long-poll Telegram for the owner's"
  "toolchain.sh","toolchain.sh — the provider wrappers (W0033). A typed front door to the tools"
  "utgard.sh","utgard.sh — the sealed execution barrier (Utgard, the realm outside the wall)."
  "valhalla.sh","valhalla.sh — the process hall (Valhalla). List, inspect, revive, and read logs"
  "valknut-load.sh","valknut-load.sh — Valknut, the knot that binds the repo distro into each tool's"
  "vor-crew-state.sh","vor-crew-state.sh - deterministic read of an Eindri worker's CURRENT state."
  "wedge-notify.sh","wedge-notify.sh — the Ymir wedge-alarm notifier."
  "workspace-provision.sh","workspace-provision.sh — carve one workspace for the operator. Single tenant:"
  "workspace-rag.sh","workspace-rag.sh — memory over the realm workspaces (W0038)."
  "wyrd-db.sh","wyrd-db.sh — the database layer (W0040). Applies the platform schema and reports"
  "yggdrasil.sh","yggdrasil.sh — the worktree manager (Yggdrasil, the world-tree)."
  "ymir-edit.sh","ymir-edit.sh — open a file in the Allfather's editor, on its own desktop."
  "ymir-install.sh","ymir-install.sh — THE FIRST SETUP. Stand the full Ymir up for the operator:"
  "ymir-migrate.sh","ymir-migrate.sh — versioned STRUCTURE migrations for existing Ymir homes."
  "ymir-platform.sh","ymir-platform.sh — the portability layer."
  "ymir-say.sh","ymir-say.sh — let Ymir speak on the Allfather's desktop."
  "ymir-validate.sh","ymir-validate.sh — verify the installation actually works."
