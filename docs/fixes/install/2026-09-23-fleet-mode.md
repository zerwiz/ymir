## install · unversioned · 2026-09-23 — FLEET MODE for users

### Why
The Allfather's word: push Ymir to npm, with a setup option so npm users can
run the seat in FLEET mode — the federation's surfaces raised and the harness
pointed at the shared well.

### What
- `--fleet` on `bin/ymir-install.sh` — seats the fleet services during setup.
- `bin/ymir-fleet.sh` — one command for users: `status` / `ensure [--well-url]`
  (raises well-MCP · A2A node · mill · stone · cards; points pi/opencode at
  the well).
- `package.json` version 0.1.40 for the publish.

### Files
- `bin/ymir-fleet.sh` · `bin/ymir-install.sh` · `package.json`
