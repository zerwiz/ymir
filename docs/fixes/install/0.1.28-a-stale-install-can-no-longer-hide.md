## install · 0.1.28 · 2026-09-18 — a stale install can no longer hide

### Why
npm install -g reported 'changed 200 packages' while changing nothing: it resolved a cached latest.

### Files
- `bin/ymir.js`
- `install.sh`