## skills · unversioned · 2026-09-19 — the skald's second hand: translation, a craft both Bragi and Kvasir read

### Why
- **`bragi-translation`** — a new skill, Bragi's word-craft: speech→text
  (whisper.cpp on heimdall, the proven `WhatsApp Ptt` transcription of today)
  then text→text (the llama router's local seats, no API key). The detailed
  `assets/programs.md` names every program, its seat, and the exact commands —
  the opus→wav trap, the model table, the router recipe, the record law.
- **Both figures read it** — Bragi's marketing skill (§5) points to the craft
  for a multilingual campaign; Kvasir's agent card maps it (the scout surveys
  the translation terrain, changing nothing). The registry gains the row
  (`skills[27]`); the hoard gains the folder
  `hodd/docs/translation/` (README + the record index).
- **Also mended:** the compliance gate's `app-provided` exemption dropped the
  marked skill from only one side of the index comparison — the smithy's
  SKILL.md is found on disk through the symlink, so the real-side had to
  forget it too. The gate passes 15/15 again.

galdr-reread: skill index (skills[27] + bragi-translation row) — no owned asset
changed (the skill is its own reference; `assets/programs.md` is its detail).

### Files
- `assets/programs.md`
