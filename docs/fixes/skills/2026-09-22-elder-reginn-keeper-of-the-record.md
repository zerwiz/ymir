## skills · unversioned · 2026-09-22 — the Elder (Reginn): keeper of the record

### Why
The hoard's record — the plan ledger, the documentation shelves, the memory
(well, runes, daily) — had no owning seat. Plans piled in a retired shelf
(`memory/plans/`), a mirror grew in the public tree, and a new federation plan
was filed without first reading the elders (37, 39, 36, 09/25–27); this session
caught all three and folded them (fix note
`2026-09-22-the-plan-ledger-is-canonical.md`). The keeper seat makes the
discipline structural: read the elders before write, reconcile never contradict.

### What
- **`elder`** — a new figure (Reginn, the keeper who remembers): read-only
  records council for plans, docs, and memory; drafts/findings only, all
  changes through the normal gates and the append-only law.
- **`elder-home` skill** — the doctrine: the ledger law (canonical shelf,
  numbering, README rows, Part-0 reconciliation, retired shelves) and the
  memory charter (well recall/observe, runes chain single-writer, daily logs).
- Registered: skill index (28), roster (`config/agents.yaml.example`), the
  registry, and the naming map.

### Files
- `.agents/agents/elder.md` (new)
- `.agents/skills/elder-home/SKILL.md` + `assets/ledger.md` + `assets/memory.md` (new)
- `.agents/skills/README.md` · `config/agents.yaml.example` ·
  `.agents/assets/agents/registry.md` · `.agents/assets/agents/naming.md`
