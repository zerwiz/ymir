## runtime · 0.1.52 · 2026-09-23 — the tree declares the version the fleet is on

### Why
- **Problem:** the fleet had drifted into three versions with no alarm: the tree
  said **0.1.39**, npm `latest` was **0.1.50**, and this box had **0.1.45**
  installed. Law 5 says the fleet is versioned together.
- **Fix (Allfather's word, 2026-09-23):** **0.1.52**.
  - The machine now runs the published latest: `npm install -g @zerwiz/ymir@latest`
    → `@zerwiz/ymir@0.1.52`; `ymir --version` reports **0.1.52**.
  - The tree (`package.json`) declares **0.1.52**, so the tree, the install, and
    the published line agree.
  - `bin/fleet-version.sh` (landed this session) now reads **in sync** instead of
    **drift** — the drift it was built to catch is closed.

### Verified
```
npm ls -g | @zerwiz/ymir@0.1.52
ymir --version → 0.1.52
bin/fleet-version.sh → tree 0.1.52 · installed 0.1.52 · published 0.1.52 · in sync
```

### Files
- `package.json`
