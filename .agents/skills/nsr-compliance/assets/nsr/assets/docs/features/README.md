# docs/features/

Per-feature deep-dive specs. One file per feature.

## Files

- *(add one per feature, e.g., `<feature-name>.md`)*

## Template Sections

Each feature spec must cover: Overview · Schema · Endpoints · Edge Cases · Acceptance Criteria · Skills.

## Rules

- Every feature in `FEATURES.md` (root) has a matching spec here.
- Include schemas, params, edge cases, acceptance criteria.