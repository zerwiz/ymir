# Features — __PROJECT__

Master registry. No feature may be implemented without being registered here, and every registered feature ships automation under `.agents/skills/features/<feature>/`.

## Registered Features

| Status | Feature | Description | Skill Dir |
|--------|---------|-------------|-----------|
| 🚧 Planned | *(add feature)* | *(description)* | `.agents/skills/features/<feature>/` |

## Rules

- Every feature has a matching skill directory under `.agents/skills/features/`.
- Every feature maps to a stack in `TECH_STACK.md`.
- Keep in sync via `.compliance/gates/verify_docs.py`.