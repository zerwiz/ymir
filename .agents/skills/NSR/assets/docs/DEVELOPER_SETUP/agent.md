# DEVELOPER_SETUP — Agent Runtime

- Install harness via `.compliance/installer/install.sh`.
- Embedded compliance: `.agents/skills/NSRcompliance/` (SKILL.md, config, gates, harness, telemetry).
- MCP sync → WayOfTeams (Kanban, Knowledge Base, Anchor Memory).
- Env-driven config only; run `check_env.sh` before tasks.
- Telemetry via `.compliance/telemetry/logger.py`.