# Env: development

# Copy to .env for local dev — inject real values, never commit.
APP_NAME=__project__
APP_ENV=development
APP_PORT=3000

DATABASE_URL=postgres://app:app@localhost:5432/app
REDIS_URL=redis://localhost:6379

FEATURES_ENABLED=

CLIENT_ID=dev-client
TENANT_ID=dev-tenant

# Inject at runtime; never commit.
# CRYPTO_SECRET=