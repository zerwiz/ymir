# Tenant: <name>

# Base config for a tenant in a shared multi-tenant deployment.
APP_MODE=multi-tenant
FEATURES_ENABLED=

# Tenant identity resolved per-request from token claims.
# TENANT_SIGNING_KEY=  (inject via secret manager)