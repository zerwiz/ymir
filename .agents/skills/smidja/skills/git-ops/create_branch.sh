#!/usr/bin/env bash
# STUB: create branch with naming + ticket link.
set -e
ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
echo "[git-ops/create_branch] stub: git checkout -b feat/<feature>-<ticket>"
exit 0