# installer/ — __PROJECT__

Self-install tooling for the deterministic harness.

## Files
- `install.sh` — install the harness into a repo.

## Rules
- Single repeatable command to install `.compliance/`.
- Installs config, gates, harness, envelopes, telemetry.