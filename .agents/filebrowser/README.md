# SKRYMIR — Web File Browser Config

Web-based drive interface for users to view, upload, and edit workspace files.

## Planned Files
- `.filebrowser.json` — root config mapping `/files` route (exists)
- `filebrowser.db` — user auth, roles, scoped permissions (created at runtime)

## Scoping
- `./svartalfaheim` mounted at `/srv/realms`
- `./midgard` mounted at `/srv/shared`