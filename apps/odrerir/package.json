{
  "name": "@ymir/odrerir",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "\u00d3\u00f0rerir \u2014 the Ymir fleet's board on its own deck: the landing's carved cloth, the board's machinery, read-only.",
  "main": "electron/main.cjs",
  "scripts": {
    "check:cloth": "node scripts/extract-cloth.mjs --check",
    "dev": "npm run check:cloth && astro dev",
    "build": "npm run check:cloth && astro build",
    "preview": "astro preview",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "astro": "latest"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/zerwiz/odrerir.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
