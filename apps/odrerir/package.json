{
  "name": "@zerwiz/odrerir",
  "type": "module",
  "version": "0.2.0",
  "private": false,
  "description": "Óðrerir — the Ymir fleet's Live Hall, rebuilt as a Hlidskjalf-type app: React + Vite, the ember cloth, the halls buttons, the tickets + plans boards in one anatomy.",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "prepack": "npm run build"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/zerwiz/odrerir.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "electron",
    "README.md"
  ]
}