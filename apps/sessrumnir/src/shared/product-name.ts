/** The desktop seat's display name — Sessrúmnir, the hall of many seats.
 *  A product name, never a translation key (ruling R9). */
export const SESSRUMNIR_PRODUCT_NAME = 'Sessrúmnir'

/** The name Pi gave this shell before Ymir adopted it. Kept only so an import
 *  that has not been walked yet cannot break the build; never render it. */
export const PI_DESKTOP_PRODUCT_NAME = SESSRUMNIR_PRODUCT_NAME
