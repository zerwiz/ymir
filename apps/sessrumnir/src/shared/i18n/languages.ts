/** The language every other language file is translated from. */
export const SOURCE_LANGUAGE = 'en'

/** Test language that marks every translated string (see pseudo.ts). */
export const PSEUDO_LANGUAGE = 'en-XA'

/** `language` setting value that follows the OS language list. */
export const SYSTEM_LANGUAGE = 'system'

/** Env var that makes the pseudo-language selectable in any build. */
export const PSEUDO_LANGUAGE_ENV = 'PI_DESKTOP_PSEUDO_LANGUAGE'
export const PSEUDO_LANGUAGE_ENV_ON = '1'
